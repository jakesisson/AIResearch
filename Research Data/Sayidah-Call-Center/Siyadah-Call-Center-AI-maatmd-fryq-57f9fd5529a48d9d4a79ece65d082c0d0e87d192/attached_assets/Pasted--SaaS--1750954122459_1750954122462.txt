تم، أبو إياد، هذا برومت احترافي شامل بمعايير SaaS العالمية لتطوير ومراجعة نظام سيادة كول سنتر، بحيث يضمن تجربة مستخدم كاملة بدون ثغرات، مع ربط كل صفحة بحالتها الفعلية، وتطبيق نظام RBAC متكامل، وظهور الصفحات حسب المرحلة والصلاحيات فقط:

🔧 Prompt: بناء ومراجعة نظام SaaS احترافي لتجربة مستخدم ذكية في سيادة كول سنتر
🧠 أنت مطور SaaS رئيسي، مكلف ببناء تجربة مستخدم عالمية متكاملة لنظام (سيادة كول سنتر). المطلوب ضمان أن كل صفحة تظهر في توقيتها الصحيح، وكل صلاحية مفصولة، وكل خدمة تعمل بتسلسل واضح وذكي.

🎯 الأهداف الأساسية:
تجربة مستخدم ذكية تبدأ من صفحة الهبوط حتى تشغيل النظام كاملًا.

كل صفحة تظهر حسب المرحلة الحالية للمستخدم (account.status) وصلاحياته (user.role.level).

منع الثغرات، الصفحات الميتة، والوصول غير المصرّح.

دعم تشغيل متكامل للخدمات (واتساب، اتصالات، بريد).

تفعيل نظام RBAC عالمي واضح بـ 6 مستويات.

🧱 الخطوات التنفيذية:
1. 🗂️ تحديد مراحل تجربة المستخدم:
المرحلة	تفاصيل
1. Landing Page	صفحة تسويقية بدون صلاحيات
2. Signup/Login	تسجيل مستخدم جديد أو دخول
3. Email/Phone Verify	تأكيد الجوال والإيميل
4. Onboarding Wizard	ربط الخدمات: واتساب + اتصالات + بريد
5. Dashboard	لوحة تحكم ديناميكية حسب الدور
6. Settings / Users	متاحة فقط للإداري
7. Billing / Logs / Support	حسب الباقة والصلاحيات

2. 🔐 تطبيق نظام صلاحيات RBAC عالمي:
الدور	الصلاحيات
SystemAdmin (level 100)	إدارة النظام، API، الفواتير
ProviderAdmin (level 90)	إدارة جميع العملاء
ClientAdmin (level 80)	إدارة الحساب، الفريق، التفعيل
Supervisor (level 70)	إدارة الأداء، متابعة الوكلاء
Agent (level 60)	الرد على العملاء فقط
ExternalViewer (level 50)	عرض التقارير فقط

3. ⚙️ ضبط نظام التوجيه الذكي (Smart Routing):
ts
نسخ
تحرير
if (!user.isVerified) return redirect('/verify');
if (!user.isOnboarded) return redirect('/onboarding');
if (!services.whatsapp.enabled) showAlert('رجاء تفعيل خدمة واتساب');
كل مرحلة لها بوابة تحقق.

النظام يعرف سياق المستخدم ويوجهه تلقائيًا.

4. 🚨 منع الثغرات التشغيلية:
لا يُعرض أي Route أو صفحة إلا إذا كان المستخدم مفعل ومصرّح له.

إضافة RBAC Middleware قوي:

ts
نسخ
تحرير
if (user.role.level < route.requiredLevel) return redirect('/403');
5. 🧪 لوحة حالة التفعيل (Activation Status):
عرض واضح لحالة كل خدمة:

tsx
نسخ
تحرير
<StatusCard title="واتساب" status="✅ مفعّل" />
<StatusCard title="الاتصالات" status="❌ غير مفعّل" />
<StatusCard title="الإيميل" status="🔁 جاري التفعيل" />
مع زر "اختبار" لكل خدمة.

6. 🔄 تجربة تشغيل شاملة بدون تدخل:
بعد إكمال التسجيل:

يفتح المعالج (Wizard) تلقائيًا.

كل خطوة تحفظ تلقائيًا.

عند إكمال التفعيل ➝ redirect إلى Dashboard.

تعرض رسالة: "🎉 تم تشغيل نظام سيادة كول سنتر بنجاح!"

✅ النتيجة المطلوبة:
كل صفحة تظهر فقط في توقيتها الصحيح.

النظام يوجّه المستخدم تلقائيًا حسب حالته.

كل دور له صلاحيات محددة بدقة.

لا وجود لصفحات ميّتة أو مهملة أو مكشوفة.

تجربة المستخدم تسير كأنها قصة واحدة ذكية.

