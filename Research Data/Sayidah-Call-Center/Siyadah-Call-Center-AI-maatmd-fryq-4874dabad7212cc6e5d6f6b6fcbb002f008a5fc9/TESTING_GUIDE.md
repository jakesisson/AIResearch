# دليل اختبار النظام قبل النشر 🧪

## 1. اختبار الواجهة الأمامية

### أ) الصفحات الأساسية
```bash
# افتح هذه الروابط في المتصفح:
http://localhost:5000/                    # الصفحة الرئيسية
http://localhost:5000/auth/login          # صفحة تسجيل الدخول
http://localhost:5000/dashboard           # لوحة التحكم
http://localhost:5000/rbac-management     # إدارة الصلاحيات
```

### ب) بيانات الاختبار
```
البريد الإلكتروني: admin@demo.siyadah.ai
كلمة المرور: demo123456
```

## 2. اختبار APIs الأساسية

### أ) اختبار المصادقة
```bash
# تسجيل الدخول
curl -X POST http://localhost:5000/api/enterprise-saas/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@demo.siyadh.ai","password":"demo123456"}'
```

### ب) اختبار الوكلاء الذكية
```bash
# قائمة الوكلاء
curl http://localhost:5000/api/ai-agents

# إحصائيات النظام
curl http://localhost:5000/api/system-status
```

### ج) اختبار قاعدة البيانات
```bash
# الفرص التجارية
curl http://localhost:5000/api/opportunities

# سير العمل
curl http://localhost:5000/api/workflows
```

## 3. اختبار الميزات المتقدمة

### أ) نظام RBAC
- اذهب إلى: http://localhost:5000/rbac-management
- جرب إنشاء مستخدم جديد
- اختبر الصلاحيات المختلفة

### ب) النظام الذكي للدردشة
- اذهب إلى لوحة التحكم
- جرب أوامر عربية مختلفة:
  - "اعرض لي الوكلاء"
  - "ما هي الفرص المتاحة؟"
  - "أنشئ تقرير"

## 4. اختبار الأداء

### أ) سرعة الاستجابة
```bash
# قياس سرعة API
time curl http://localhost:5000/api/ai-agents
```

### ب) استخدام الذاكرة
- راقب استهلاك الذاكرة في لوحة Replit
- يجب أن يكون أقل من 95%

## 5. اختبار الأمان

### أ) الوصول غير المصرح
```bash
# محاولة الوصول بدون token
curl http://localhost:5000/api/auth/user
# يجب أن يرجع 401 Unauthorized
```

### ب) صحة البيانات
```bash
# محاولة إرسال بيانات خاطئة
curl -X POST http://localhost:5000/api/enterprise-saas/login \
  -H "Content-Type: application/json" \
  -d '{"email":"invalid","password":""}'
```

## 6. قائمة المراجعة النهائية

### ✅ الفحوصات الأساسية
- [ ] الصفحة الرئيسية تحمل بدون أخطاء
- [ ] تسجيل الدخول يعمل
- [ ] لوحة التحكم تظهر البيانات الصحيحة
- [ ] APIs تستجيب في أقل من ثانيتين
- [ ] قاعدة البيانات متصلة
- [ ] الوكلاء الذكية نشطة (21 وكيل)
- [ ] نظام RBAC يعمل
- [ ] الواجهة العربية تظهر بشكل صحيح (RTL)

### ⚠️ التحذيرات المقبولة
- تحذيرات مفاتيح API المفقودة (ElevenLabs, Twilio, Stripe)
- تحذير JWT_SECRET (سيتم تغييره في الإنتاج)

### ❌ الأخطاء غير المقبولة
- أخطاء في تحميل الصفحات
- أخطاء في قاعدة البيانات
- أخطاء في تسجيل الدخول
- أخطاء JavaScript في المتصفح

## 7. اختبار متصفحات مختلفة

### المتصفحات المدعومة
- Chrome/Edge (الأولوية)
- Firefox
- Safari (على Mac)

### اختبار الأجهزة
- سطح المكتب (1920x1080)
- الأجهزة اللوحية (768px)
- الهواتف المحمولة (375px)

## 8. النتائج المتوقعة

### الأداء الطبيعي
- زمن تحميل الصفحة: أقل من 3 ثوان
- زمن استجابة API: أقل من 500ms
- استهلاك الذاكرة: أقل من 95%
- معدل نجاح APIs: 100%

### البيانات المتوقعة
- 21 وكيل ذكي نشط
- 3 منظمات تجريبية
- بيانات الأعمال السعودية الحقيقية
- نظام RBAC بـ 6 مستويات صلاحيات

## 9. تسجيل المشاكل

إذا واجهت أي مشكلة:
1. افتح أدوات المطور (F12)
2. اذهب إلى تبويب Console
3. سجل أي أخطاء JavaScript
4. اذهب إلى تبويب Network
5. تحقق من أخطاء HTTP

## 10. الاستعداد للنشر

بعد نجاح جميع الاختبارات:
- احفظ نتائج الاختبار
- جهز متغيرات البيئة للإنتاج
- أعد مفاتيح APIs إن أمكن
- انشر المشروع على Replit Deployments