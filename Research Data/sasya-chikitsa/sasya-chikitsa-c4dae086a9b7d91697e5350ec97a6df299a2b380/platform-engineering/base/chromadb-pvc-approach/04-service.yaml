apiVersion: v1
kind: Service
metadata:
  name: chromadb-service
  namespace: your-namespace  # Replace with your actual namespace
  labels:
    app: chromadb
spec:
  selector:
    app: chromadb
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
