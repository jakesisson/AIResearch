// Middleware removed - Clerk authentication no longer required
// This file kept to maintain Next.js structure but middleware is disabled

export default function middleware() {
  // No authentication middleware needed
}

export const config = {
  matcher: [],
};