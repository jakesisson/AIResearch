# Code generated by schema2code at 2025-10-20 19:11:11. DO NOT EDIT.


from typing import List, Dict, Optional, Any, Union
from datetime import datetime, date, time, timedelta
from pydantic import BaseModel, Field, AnyUrl, EmailStr, conint, confloat



class DynamicTool(BaseModel):
    """Schema for DynamicTool object that implements LangChain BaseTool interface"""
    id: Optional[int] = Field(default=None, description="The unique identifier for the tool")
    """The unique identifier for the tool"""
    user_id: str = Field(..., description="The unique identifier for the user")
    """The unique identifier for the user"""
    embedding: Optional[List[float]] = Field(default=None, description="The tool's embedding vector")
    """The tool's embedding vector"""
    code: str = Field(..., description="The tool's implementation code")
    """The tool's implementation code"""
    function_name: str = Field(..., description="The name of the function to be called")
    """The name of the function to be called"""
    created_at: Optional[datetime] = Field(default=None, description="The timestamp when the tool was created")
    """The timestamp when the tool was created"""
    updated_at: Optional[datetime] = Field(default=None, description="The timestamp when the tool was last updated")
    """The timestamp when the tool was last updated"""
    name: str = Field(..., description="The unique name of the tool that clearly communicates its purpose")
    """The unique name of the tool that clearly communicates its purpose"""
    description: str = Field(..., description="Used to tell the model how/when/why to use the tool. You can provide few-shot examples as a part of the description")
    """Used to tell the model how/when/why to use the tool. You can provide few-shot examples as a part of the description"""
    args_schema: Optional[Union[Any, Dict[str, Any], str]] = Field(default=None, description="Pydantic model class to validate and parse the tool's input arguments")
    """Pydantic model class to validate and parse the tool's input arguments"""
    return_direct: Optional[bool] = Field(default=False, description="Whether to return the tool's output directly. Setting this to True means that after the tool is called, the AgentExecutor will stop looping")
    """Whether to return the tool's output directly. Setting this to True means that after the tool is called, the AgentExecutor will stop looping"""
    tags: Optional[List[str]] = Field(default_factory=lambda: [], description="Optional list of tags associated with the tool. These tags will be associated with each call to this tool and passed as arguments to the handlers defined in callbacks")
    """Optional list of tags associated with the tool. These tags will be associated with each call to this tool and passed as arguments to the handlers defined in callbacks"""
    metadata: Optional[Dict[str, Any]] = Field(default_factory=lambda: {}, description="Optional metadata associated with the tool. This metadata will be associated with each call to this tool and passed as arguments to the handlers defined in callbacks")
    """Optional metadata associated with the tool. This metadata will be associated with each call to this tool and passed as arguments to the handlers defined in callbacks"""
    handle_tool_error: Optional[Union[bool, str, Any]] = Field(default=False, description="Handle the content of the ToolException thrown. Can be a boolean, string, or callable function")
    """Handle the content of the ToolException thrown. Can be a boolean, string, or callable function"""
    handle_validation_error: Optional[Union[bool, str, Any]] = Field(default=False, description="Handle the content of the ValidationError thrown. Can be a boolean, string, or callable function")
    """Handle the content of the ValidationError thrown. Can be a boolean, string, or callable function"""
    response_format: Optional[str] = Field(default='content', description="The tool response format. If 'content' then the output is interpreted as ToolMessage contents. If 'content_and_artifact' then output is expected to be a two-tuple of (content, artifact)")
    """The tool response format. If 'content' then the output is interpreted as ToolMessage contents. If 'content_and_artifact' then output is expected to be a two-tuple of (content, artifact)"""
    parameters: Optional[Dict[str, Any]] = Field(default=None, description="Legacy parameters field - use args_schema for new implementations")
    """Legacy parameters field - use args_schema for new implementations"""

    class Config:
        extra = "ignore"