$schema: http://json-schema.org/draft-07/schema#
title: Config
description: Application configuration
type: object
properties:
  server:
    $ref: server_config.yaml
  database:
    $ref: database_config.yaml
  redis:
    $ref: redis_config.yaml
  rabbitmq:
    $ref: rabbitmq_config.yaml
  auth:
    $ref: auth_config.yaml
  inference_services:
    $ref: inference_service_config.yaml
  summarization:
    $ref: summarization_config.yaml
  memory:
    $ref: memory_config.yaml
  web_search:
    $ref: web_search_config.yaml
  preferences:
    $ref: preferences_config.yaml
  model_profiles:
    $ref: model_profile_config.yaml
  refinement:
    $ref: refinement_config.yaml
  image_generation:
    $ref: image_generation_config.yaml
  circuit_breaker:
    $ref: circuit_breaker_config.yaml
  gpu_config:
    $ref: gpu_config.yaml
  log_level:
    type: string
    description: Log level
  internal:
    $ref: internal_config.yaml
  event_stream:
    $ref: event_stream_config.yaml
required:
  - server
  - database
  - redis
  - auth
  - inference_services
  - summarization
  - memory
  - web_search
  - preferences
  - model_profiles
  - refinement
  - image_generation
  - circuit_breaker
  - gpu_config
  - log_level
  - rabbitmq
  - internal_config
