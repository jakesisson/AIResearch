$schema: http://json-schema.org/draft-07/schema#
title: UserConfig
description: User-specific configuration
type: object
properties:
  user_id:
    type: string
    description: User ID
  summarization:
    $ref: summarization_config.yamls
  memory:
    $ref: memory_config.yaml
  web_search:
    $ref: web_search_config.yaml
  preferences:
    $ref: preferences_config.yaml
  model_profiles:
    $ref: model_profile_config.yaml
  refinement:
    $ref: refinement_config.yaml
  image_generation:
    $ref: image_generation_config.yaml
  circuit_breaker:
    $ref: circuit_breaker_config.yaml
  gpu_config:
    $ref: gpu_config.yaml
  workflow:
    $ref: workflow_config.yaml
  tool:
    $ref: tool_config.yaml
  event_stream:
    $ref: event_stream_config.yaml
  context_window:
    $ref: context_window_config.yaml
required:
  - user_id
  - summarization
  - memory
  - web_search
  - preferences
  - model_profiles
  - refinement
  - image_generation
  - circuit_breaker
  - gpu_config
  - workflow
  - tool
  - context_window
