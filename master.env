# Master Environment Configuration
# This file contains all environment variables used across all research projects
# Projects should source this file or use it as a template

# ============================================================================
# AI/LLM Configuration - Azure OpenAI (PRIMARY)
# ============================================================================
# All projects should use Azure OpenAI as the model provider
MODEL_PROVIDER=azure_openai
MODEL_ID=gpt-4.1
AZURE_OPENAI_API_KEY=EBV6HXBgllfMaegY1CzWcRVfCMLgXeSLbnnNNqIYaiJf28gVPcePJQQJ99BKACYeBjFXJ3w3AAAAACOG60w6
AZURE_OPENAI_ENDPOINT=https://ksontini-mcp-project.openai.azure.com/
AZURE_OPENAI_API_INSTANCE=ksontini-mcp-project
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# Model Parameters
MAX_TOKENS=1000
TEMPERATURE=0.3
TOP_P=0.4

# Legacy/Alternative API Keys (for compatibility, but Azure OpenAI should be used)
OPENAI_API_KEY=${OPENAI_API_KEY:-}
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
GOOGLE_AI_API_KEY=${GOOGLE_AI_API_KEY:-}
AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-}
AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-}
AWS_REGION=${AWS_REGION:-us-east-1}
BEDROCK_REGION=${BEDROCK_REGION:-us-east-1}

# ============================================================================
# Database Configuration - PostgreSQL (STANDARD)
# ============================================================================
# All projects should use PostgreSQL as the primary database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=test_user
DB_PASSWORD=test_password
DB_NAME=test_db

# PostgreSQL Connection Strings
DATABASE_URL=postgresql://test_user:test_password@localhost:5432/test_db
POSTGRES_URL=postgresql://test_user:test_password@localhost:5432/test_db
POSTGRES_USER=test_user
POSTGRES_PASSWORD=test_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=test_db

# Legacy MongoDB (for compatibility only - migrate to PostgreSQL)
MONGODB_URI=${MONGODB_URI:-mongodb://localhost:27017}
MONGODB_DB_NAME=${MONGODB_DB_NAME:-default}

# Redis (for caching/sessions)
REDIS_URL=redis://localhost:6379

# ============================================================================
# Vector Store / Embeddings
# ============================================================================
PINECONE_API_KEY=${PINECONE_API_KEY:-}
PINECONE_ENVIRONMENT=${PINECONE_ENVIRONMENT:-}
WEAVIATE_URL=${WEAVIATE_URL:-}
QDRANT_URL=${QDRANT_URL:-}
CHROMA_URL=${CHROMA_URL:-}

# ============================================================================
# LangSmith / LangChain Configuration (OPTIONAL)
# ============================================================================
# NOTE: LangChain API keys are OPTIONAL - only needed for LangSmith tracing/monitoring
# Your projects will work fine without these. Only add if you want observability.

# LangSmith credentials for tracing and monitoring (optional)
# Get free account at: https://smith.langchain.com
LANGCHAIN_API_KEY=${LANGCHAIN_API_KEY:-}
LANGCHAIN_TRACING_V2=${LANGCHAIN_TRACING_V2:-false}
LANGCHAIN_ENDPOINT=${LANGCHAIN_ENDPOINT:-https://api.smith.langchain.com}
LANGCHAIN_PROJECT=${LANGCHAIN_PROJECT:-research-projects}

# LangChain Hub (optional - for sharing prompts/chains)
LANGCHAIN_HUB_API_URL=${LANGCHAIN_HUB_API_URL:-https://api.hub.langchain.com}
LANGCHAIN_HUB_API_KEY=${LANGCHAIN_HUB_API_KEY:-}

# ============================================================================
# Observability / Monitoring
# ============================================================================
OTEL_SERVICE_NAME=${OTEL_SERVICE_NAME:-research-project}
OTEL_SERVICE_VERSION=${OTEL_SERVICE_VERSION:-1.0.0}
OTEL_EXPORTER_OTLP_ENDPOINT=${OTEL_EXPORTER_OTLP_ENDPOINT:-}
OTEL_EXPORTER_OTLP_HEADERS=${OTEL_EXPORTER_OTLP_HEADERS:-}
OTEL_RESOURCE_ATTRIBUTES=${OTEL_RESOURCE_ATTRIBUTES:-}
OTEL_SAMPLING_RATIO=${OTEL_SAMPLING_RATIO:-1.0}
NEXT_OTEL_VERBOSE=${NEXT_OTEL_VERBOSE:-false}

# ============================================================================
# Application Configuration
# ============================================================================
APP_ENV=dev
NODE_ENV=${NODE_ENV:-development}
PYTHON_ENV=${PYTHON_ENV:-development}
DEBUG=${DEBUG:-false}
LOG_LEVEL=INFO
PORT=${PORT:-8000}
HOST=${HOST:-0.0.0.0}

# ============================================================================
# Discord / Social Platforms
# ============================================================================
DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN:-}
DISCORD_ADMIN_USER_ID=${DISCORD_ADMIN_USER_ID:-}
DISCORD_GUILD_ID=${DISCORD_GUILD_ID:-}

# ============================================================================
# External Services
# ============================================================================
BROWSERBASE_API_KEY=${BROWSERBASE_API_KEY:-}
E2B_API_KEY=${E2B_API_KEY:-}
INNGEST_EVENT_KEY=${INNGEST_EVENT_KEY:-}
INNGEST_SIGNING_KEY=${INNGEST_SIGNING_KEY:-}

# ============================================================================
# Feature Flags
# ============================================================================
ENABLE_HEALTH_CHECK=${ENABLE_HEALTH_CHECK:-true}
ENABLE_METRICS=${ENABLE_METRICS:-true}
ENABLE_TRACING=${ENABLE_TRACING:-false}

# ============================================================================
# Development / Debug
# ============================================================================
DEBUG_AIDER=${DEBUG_AIDER:-false}
VERBOSE_LOGGING=${VERBOSE_LOGGING:-false}

# ============================================================================
# Security
# ============================================================================
SECRET_KEY=${SECRET_KEY:-}
JWT_SECRET=${JWT_SECRET:-}
SESSION_SECRET=${SESSION_SECRET:-}

# Langfuse (observability / cost-perf tests)
LANGFUSE_SECRET_KEY=sk-lf-df47be0e-1c13-4a31-8e4d-5572488c6479
LANGFUSE_PUBLIC_KEY=pk-lf-49156185-c79f-41f5-b9eb-9a7dea4f886d
LANGFUSE_BASE_URL=https://us.cloud.langfuse.com

# WxFlows (required for Agente-de-IA cost-perf test - tools are loaded at startup)
# Get endpoint/apikey from your WxFlows project; leave empty to skip or mock.
WXFLOWS_ENDPOINT=
WXFLOWS_APIKEY=
